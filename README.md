# WUREC — Wharton Undergraduate Real Estate Club website

A static remake of the former club website at **wurec.info** (previously a
Squarespace site). No framework, no build dependencies beyond Python 3: plain
HTML, CSS and a little JavaScript, ready for GitHub Pages or any static host.

## Site map

The original URL slugs are preserved so old links keep working:

| Page | Path | Original title |
| --- | --- | --- |
| Home | `/` | WUREC |
| Membership | `/membership/` | Membership — WUREC |
| Treks (featured) | `/new-page/` | Treks (Featured) — WUREC |
| Treks (all) | `/treks/` | Treks — WUREC |
| Speakers | `/new-page-1/` | Speakers (Linked) — WUREC |
| Case Comps | `/new-page-2/` | Case Comps (Featured) — WUREC |
| Executive Board | `/exec-board/` | Executive Board — WUREC |

## Editing the site

```
src/layout.html      shared <head>, header/nav and footer
src/pages/*.html     one file per page (front matter + body)
assets/css/site.css  styles; palette, type and role tokens are the :root block at the top
assets/js/site.js    mobile menu, sticky header, reveal-on-scroll, filmstrip
assets/img/          artwork (placeholders generated by tools/make_placeholders.py)
assets/img/carousel/ club photos used in heroes and the home filmstrip, generated from carousel/
assets/img/photos/   trek and event photos with descriptive names, generated from the originals in assets/
assets/img/og/       link-preview images, generated by tools/make_brand.py
carousel/            original photos
tools/build.py       renders src/pages/* into the root as <slug>/index.html, plus sitemap.xml,
                     robots.txt, vercel.json, 404.html and the redirect pages for old addresses
tools/make_carousel.py  resizes carousel/ photos into assets/img/carousel/
tools/make_photos.py    resizes the photos in assets/ into assets/img/photos/
tools/make_brand.py     favicon, touch icon, logo and link-preview images (Pillow + fontTools)
tools/make_headshots.py trims and squares board portraits into exec-board/pictures/
tools/board.py          reads src/data/board.json and renders the Executive Board roster
tools/sync_board.py     refreshes board.json and the headshots from Google Sheets / Drive
src/data/board.json     the Executive Board roster (season, committees, members)
.github/workflows/sync-board.yml  runs sync_board.py + build.py daily and commits the result
```

1. Edit a page in `src/pages/` (or the nav list at the top of `tools/build.py`).
2. Run `python3 tools/build.py`.
3. Commit both the source and the generated `index.html` files.

Inside page bodies use `{{root}}` for links and asset paths
(e.g. `{{root}}membership/`, `{{root}}assets/img/photo.jpg`) so pages work
at any depth and under any base path.

## Updating the Executive Board

The board page is generated from a Google Sheet, so board members can update
it without touching the repository. Every morning (and whenever someone
presses *Run workflow* on the **Sync executive board** action) a GitHub
Action reads the sheet and the headshot folder, rewrites
`src/data/board.json` and `exec-board/pictures/`, rebuilds the site and
commits. Vercel deploys the commit. Nothing on the site changes until the
sheet is valid: a missing column, a bad email or a row with no name stops
the run, and the log lists every problem.

### For board members

- **Sheet:** one row per person, in the order they should appear. Columns:
  `Committee`, `Name`, `School`, `Class of`, `Email`, `Bio` (optional, a short
  line under the name), `Photo` (optional, see below). Rows with the same
  Committee are grouped together under that heading, in first-appearance
  order. Blank rows are ignored.
- **Season:** the year on the page (e.g. `2026–2027`) comes from a tab called
  `Settings` with a `Season` row (column A `Season`, column B the value). If
  there is no such tab the current year stays.
- **Photos:** drop a photo into the shared Drive folder named after the person
  exactly as in the sheet, e.g. `Josh Kwon.jpg` (`.png`, `.webp` and `.heic`
  also work; capitalisation, spaces and accents don't matter). To use a
  different file name, put it in the person's `Photo` column. The sync trims
  any flat frame, centre-crops to a square and shrinks to 512×512, so upload
  the original. A person with no photo gets a grey placeholder. Photos of
  people no longer in the sheet are removed from the site.

### One-time setup

1. **Google Cloud API key.** In [console.cloud.google.com](https://console.cloud.google.com/)
   create a project, enable the *Google Sheets API* and *Google Drive API*
   (APIs & Services → Library), then create an API key (APIs & Services →
   Credentials) and restrict it to those two APIs. The key only ever reads.
2. **Share the sheet and the folder** as *Anyone with the link → Viewer*
   (API keys can only read public files). Board members who edit get
   *Editor* access individually. The emails and photos are published on the
   site anyway, so nothing new becomes public.
3. **Repository secrets** (Settings → Secrets and variables → Actions):
   `GOOGLE_API_KEY`, `BOARD_SHEET_ID` (the long id in the sheet's URL,
   `/spreadsheets/d/<id>/edit`) and `BOARD_DRIVE_FOLDER_ID` (the id in the
   folder's URL, `/folders/<id>`).
4. Run the action once by hand (Actions → Sync executive board → Run
   workflow) and check the page.

To run the same thing locally, or to check a sheet without changing anything:

```
python3 -m pip install pillow
GOOGLE_API_KEY=... BOARD_SHEET_ID=... BOARD_DRIVE_FOLDER_ID=... python3 tools/sync_board.py --check
python3 tools/sync_board.py && python3 tools/build.py
```

`src/data/board.json` can also be edited by hand and rebuilt with
`tools/build.py` if Google is ever out of the picture.

## Preview locally

```
python3 -m http.server 8000
```

Then open <http://localhost:8000/>.

## Deploy

The site is deployed on **Vercel** straight from this repository with no build
step: framework preset *Other*, no build command, output directory `.` (the
repository root). `vercel.json`, written by `tools/build.py`, turns on trailing
slashes so every page has one address, serves real 308 redirects for the old
addresses in `REDIRECTS`, sends requests for `src/`, `tools/` and `carousel/`
back to the home page, and sets cache and security headers. Vercel shows
`404.html` for any missing address.

Domains: add `wurec.org` and `www.wurec.org` under the project's Domains
settings (Vercel redirects one to the other). To keep old links working, add
`wurec.info` there too and set it to redirect to `wurec.org`.

**GitHub Pages** also works: Settings → Pages → *Deploy from a branch*, this
branch, `/ (root)`; `.nojekyll` is included. There the redirect page in each
old address's folder does the job of `vercel.json`, and `404.html` is picked
up automatically.

Any other static host (Netlify, Cloudflare Pages) can serve the repository
root as-is.

## Search engines

`tools/build.py` writes everything search engines and link previews need. Set
`SITE_URL` at the top of it to the address the site is served from (it is
`https://wurec.org`) and rebuild before deploying.

- Every page has a canonical link, a description, Open Graph and Twitter card
  tags, a preloaded hero image and JSON-LD structured data (Organization,
  WebSite, WebPage and breadcrumbs).
- `sitemap.xml` lists every page with its last-modified date and hero image;
  `robots.txt` points to it and keeps crawlers out of `src/`, `tools/` and
  `carousel/`.
- `404.html` is the page shown for a missing address.
- The link-preview images in `assets/img/og/`, the favicon and the logo used
  in structured data come from `python3 tools/make_brand.py` (needs Pillow and
  fontTools; it downloads Cormorant Garamond and Jost into `tools/.fonts/`).
  Re-run it after changing a page's `hero:` photo.

After deploying, add the site to Google Search Console and Bing Webmaster
Tools and submit `https://wurec.org/sitemap.xml`; both then report what is
indexed and flag any errors.

## Things to fill in

The original site could not be fetched from the build environment, so the
structure and copy were reconstructed from the club's indexed pages. Replace
the placeholders below with the club's own material:

- **Speaker and case-comp tiles** — the images on the speaker and case-comp
  pages are dark SVG placeholders in `assets/img/`. Drop in real photos and
  update the `<img src>` paths in `src/pages/`. (Past treks are listed as
  text on purpose; the trek photos live in the gallery above the list.)
- **Executive Board** (`src/pages/exec-board.html`) — the 2026–27 roster and
  headshots are current. Headshots live in `exec-board/pictures/` as square
  512×512 WebPs. To add or replace one, put the portrait in a folder and run
  `python3 tools/make_headshots.py <folder>` (needs Pillow); it trims any
  flat-coloured frame, centre-crops to a square and writes the WebP. The
  comment at the top of the page explains the file naming.
- **Site title font** — the WUREC wordmark in the header and footer is set in
  Centaur, which is bundled with Microsoft Office but is not a free web font.
  Visitors without it see Cormorant Garamond (Google Fonts) instead. To show
  Centaur to everyone, add a licensed `.woff2` under `assets/fonts/` and
  declare it with `@font-face` in `assets/css/site.css`.
- **Photos** — drop photos into `carousel/`, run
  `python3 tools/make_carousel.py` (needs Pillow), then keep the filmstrip
  list in `src/pages/index.html` in step with what it prints. Page heroes
  reference the same `assets/img/carousel/` files. Photos dropped straight
  into `assets/` are resized by `python3 tools/make_photos.py` into
  `assets/img/photos/<name>.jpg`; give each one a descriptive name in that
  script's table and reference it from the pages.
- **Sign-up links** — currently point to the Wharton club sign-up page
  (`groups.wharton.upenn.edu/WUREC/club_signup`). Swap for the club's current
  membership form if it lives elsewhere.
